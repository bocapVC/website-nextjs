export interface MemberType {
  name: string;
  description: string;
  highlights: string[];
}

/** No membership-tier content exists yet — TiposDeMiembro shows an empty state until this is populated. */
export const TIPOS_MIEMBRO: MemberType[] = [];
